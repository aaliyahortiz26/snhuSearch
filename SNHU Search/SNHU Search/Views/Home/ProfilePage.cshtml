@model SNHU_Search.Models.ProfileModel

<head>
    <title>Profile Page</title>
    <link href='~/css/ProfilePage.css' rel='stylesheet'>
    <link rel="icon" href="~/favicon.ico"/>
</head>

    <div class="profleCenter">
        <div class="profilePictureDiv">
            <img class="profileImage" src="~/Images/snhu logoo.png" /><br/>

            @{
                List<string> currentUserInformation = new List<string>();
                currentUserInformation = ViewData["userProfileData"] as List<string>;

                string username;
                username = ViewData["userName"] as string;

                <label class="userInfoNameLabel">@currentUserInformation[1]</label> @*User's First Name*@
                <label class="userInfoNameLabel">@currentUserInformation[2]</label><br /> @*User's Last Name*@
                <label class="userInfoLabel">&#64;@username</label><br /> @*User's Username*@
                <label class="userInfoLabel">@currentUserInformation[0]</label><br /> @*User's Email*@
            }
        </div>
    </div>
    <button class="changePasswordButton buttonCenter" type="button" onclick=" location.href = '@Url.Action("ChangePassword", "Home")'">Change Password</button>
<script>
    var loadFile = function (event) {
        var image = document.getElementById('output');
        image.src = URL.createObjectURL(event.target.files[0]);
    };
    function submitForm() {
        document.getElementById('formid').submit();
    }
</script>

